@model ShowProductViewModel

<div class="d-flex justify-content-center">
    <ul class = "list-unstyled d-flex justify-content-center gap-3">
        <li class="display-6"><a class="text-dectoration-none" href="/">Products</a></li>
        <li class="display-6">|</li>
        <li class="display-6"><a class="text-dectoration-none" href="/categories">Categories</a></li>
    </ul>
</div>
<div class="d-flex justify-content-center">
    <div class="d-flex gap-3">
        <h1>@Model.Product.Name</h1>
    </div>
</div>
<div class="d-flex justify-content-center">
</div>
<div class="text-center d-flex justify-content-center gap-5 mt-5">
    <div class="flex-1 m-2">
        <h2>Categories</h2>
        @foreach(Association a in Model.Attributes)
        {
            <p>@a.Category.Name
            @* <form asp-action="DestroyAssociation" asp-controller= "Home" asp-route-AssociationId="@a.AssociationId" method="post">
                <input type="submit" value="Delete" class="btn btn-danger">
            </form> *@
            </p>
        }
    </div>
    <div class="vl"></div>
    <div class="flex-1 m-2">
        <h2>Add Category</h2>
        @await Html.PartialAsync("_AddCatAssociation", Model.Association)
    </div>
</div>